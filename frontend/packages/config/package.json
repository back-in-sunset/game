{
  "name": "@game/config",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint .",
    "typecheck": "tsc -b --pretty false",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^6.0.0"
  }
}
