{
  "name": "@game/api",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint .",
    "test": "vitest run",
    "typecheck": "tsc -b --pretty false",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^6.0.0"
  }
}
